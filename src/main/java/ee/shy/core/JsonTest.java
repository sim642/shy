package ee.shy.core;

import java.io.*;

public class JsonTest {
    public static void main(String[] args) throws IOException {
        /*Json json = new Json();
        Reader reader = new InputStreamReader(new FileInputStream("commitTest.json"));
        FileInputStream is = new FileInputStream("commitTest.json");
        Commit commit = json.read(is, Commit.class);
        json.write(new FileOutputStream(new File("oJsonTest.json")), commit);
        //json.write(os, Commit.class);*/

    }
}
